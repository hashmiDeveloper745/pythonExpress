#/usr/bin/python

# Python Operators Example.

# Identity OPerators:

class operators:
  x = 0
  y = 0
  def fnct(getX, getY):
    self.x = getX
    self.y = getY

    if x is y:
      print("X and Y are same Object.")
    elif x is not y:
      print("They are totally different.")
    else:
      print("X and Y have same values but they are different, in nature.")

Op = operators()
Op.fnct(63,78)

